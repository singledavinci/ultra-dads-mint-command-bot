import type { InclusionMetrics } from '../../types/inclusion';

export const inclusionMetrics: InclusionMetrics = {
    bundlesSubmitted: 0,
    bundlesIncluded: 0,
    bundlesPartialRegen: 0,
    publicBroadcasts: 0,
    protectedBroadcasts: 0,
};
